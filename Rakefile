# encoding: utf-8
namespace :get do

  desc "Retrieves doc from master branch"
  task :master do
    system "git checkout master -- doc"
  end

  desc "Retrieves doc from develop branch"
  task :develop do
    system "git checkout develop -- doc"
  end

end

